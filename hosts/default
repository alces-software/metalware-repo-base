<% nodes.each do |node| -%>
<% node.config.networks.each do |name, network| -%>
<% if network.defined -%>
<%= network.ip %> <%= network.hostname  %> <%= network.short_hostname -%> <%= node.name == 'local' ? node.config.networks[name].hostname.split(/\./).first : node.name %>
<% end -%>
<% end %>
<% end -%>
